syntax = "proto3";

package stremio.core.types;

enum Status {
  STATUS_UNSPECIFIED = 0;
  STATUS_WATCHED = 1;
  STATUS_LIKED = 2;
  STATUS_LOVED = 3;
}

message RatingInfo {
  string meta_id = 1;
  optional Status status = 2;
} 